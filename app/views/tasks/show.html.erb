<h1>Task details</h1>

<p><%= @task.title %></p>
<p><%= @task.details %></p>

<%= link_to "Back to List", tasks_path %>

<%= link_to "Edit", task_edit_path %>

<%= link_to "Delete", task_path(@task), method: :delete, data: {confirm: "sure?"} %>

